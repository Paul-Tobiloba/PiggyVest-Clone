var name = "Paul Tobiloba";
let courses = ["HTML", "CSS", "JAVASCRIPT", "nodeJs"];

console.log(name, courses);

let n = courses.length;
if (n % 2 == 0) {
    for (let i = 0; i <= 200; i++) {
        if (i % 2 == 0) {
            console.log(i)
        }
    }
};